{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch", 
            "port": 9099,
            "pathMappings": {
                "/var/www/html/Pego": "${workspaceRoot}",
            }
        }
    ]
}